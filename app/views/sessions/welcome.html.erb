<h1>Welcome!</h1>
<%if logged_in?%>
  <h1>You are logged in as <%= current_user.username %></h1>
<% else %>
  <%= button_to "Sign In", '/login', :class => 'btn btn-success', method: :get%>
  <%= button_to "Login", '/users/new', :class => 'btn btn-info', method: :get%>
<%end%>
