<% @title = 'All users' %>
<% @user.each do |t| %>
  <div class="jumbotron">
    <h2><%= t.username %> (<%= t.id %>)</h2>
    <%= link_to "More details", users_path(t), :class => 'btn btn-outline-primary' %>
  </div>
<% end %>
